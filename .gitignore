# SwiftPM
.build/
.swiftpm/

# Keep the root lockfile tracked so audited renderer dependencies stay exact.

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
dist/
*.xcuserstate

# Generated by XcodeGen (run `make app` / `xcodegen generate` to recreate)
Councis.xcodeproj/
# Pre-migration generated project; never adopt it as a second build graph.
Intatis.xcodeproj/

# macOS
.DS_Store

# Councis local runtime/worktree state
.councis/

# Predecessor state remains ignored while bounded legacy readers exist.
.intatis/

# Secrets / local config (never commit)
*.env
.env
secrets.json
Config.local.*
