// PathConfinement moved to CouncisCore so CouncisPermission can also use it
// without depending on CouncisTools. Tools reference `PathConfinement` via
// `import CouncisCore`. This file is intentionally left as a pointer only.
